name: CI

on:
  push:
    branches: [main, sprint_one]
  pull_request:
    branches: [main, sprint_one]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for SonarCloud

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'  # Cache dependencies for faster builds

      - name: Install root dependencies
        run: npm ci

      - name: Install backend dependencies
        run: npm ci
        working-directory: ./app/backend

      - name: Install frontend dependencies
        run: npm ci
        working-directory: ./app/frontend

      - name: Build backend
        run: npm run build
        working-directory: ./app/backend

      - name: Type check backend
        run: npx tsc --noEmit
        working-directory: ./app/backend

      - name: Type check frontend
        run: npx tsc --noEmit
        working-directory: ./app/frontend

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
          verbose: true

      - name: Debug Secret Visibility
        run: |
          if [ -z "${{ secrets.SONAR_TOKEN }}" ]; then
            echo "ERROR: SONAR_TOKEN is empty! GitHub cannot see the secret."
            exit 1
          else
            echo "SONAR_TOKEN is present."
          fi
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          projectBaseDir: "."
          args: >
            -Dsonar.organization=githappens
            -Dsonar.projectKey=yassineAbdellatif_Git-happens
            -X
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check SonarCloud Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true  # Don't block initially, just report
  e2e:
    runs-on: ubuntu-latest
    needs: quality

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Maestro CLI
        run: |
          curl -Ls "https://get.maestro.mobile.dev" | bash
          export PATH="$PATH:$HOME/.maestro/bin"

      - name: Run Maestro E2E tests
        run: |
          export PATH="$PATH:$HOME/.maestro/bin"
          npm run maestro:test
        continue-on-error: true  # E2E tests may need real device/simulator